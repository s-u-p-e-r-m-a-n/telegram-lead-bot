package com.example.telegramleadbot.entity.enums;
/*A draft application,
while the user hasn't yet reached the end.*/

public enum BotEventType {
    START,
    REQUEST_STARTED,
    REQUEST_COMPLETED
}