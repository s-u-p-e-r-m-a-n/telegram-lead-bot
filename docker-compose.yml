version: '3.9'

services:
  postgres:
    image: postgres:16
    container_name: telegram-lead-bot-db
    environment:
      POSTGRES_DB: telegram_lead_bot
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - telegram_lead_bot_postgres_data:/var/lib/postgresql/data

volumes:
  telegram_lead_bot_postgres_data: